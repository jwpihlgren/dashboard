import { HydrologicalMinMaxPipe } from './hydrological-min-max.pipe';

describe('HydrologicalMinMaxPipe', () => {
  it('create an instance', () => {
    const pipe = new HydrologicalMinMaxPipe();
    expect(pipe).toBeTruthy();
  });
});
