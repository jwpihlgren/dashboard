import { DegToCompassPipe } from './deg-to-compass.pipe';

describe('DegToCompassPipe', () => {
  it('create an instance', () => {
    const pipe = new DegToCompassPipe();
    expect(pipe).toBeTruthy();
  });
});
